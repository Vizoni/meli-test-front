import styled from "styled-components";

export const Container = styled.div`
	margin: 1rem;

	a, span {
		color: var(--grey);
		font-size: var(--small);
		text-decoration: none;
	}

	span {
		margin 0 0.5vw;
	}
`;
